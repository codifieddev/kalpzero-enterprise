# Config Templates

This folder holds shared templates and reference configs for the canonical
workspace.

- `eslint/base.cjs`: base lint profile for TypeScript workspaces
- `typescript/base.json`: shared compiler baseline
- `python/ruff.toml`: Python lint and formatting defaults
- `env/README.md`: environment configuration rules
